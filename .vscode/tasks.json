// .vscode/tasks.json
{
  "version": "2.0.0",
  "tasks": [
    // ————————————————
    //  Lancia il task di build per il frontend (modalità Development)
    // ————————————————
    {
      "label": "🚀 Run Dev FE",
      "type": "shell",
      "command": "npm run dev",
      "options": {
        "cwd": "${workspaceFolder}/FE"
      },
      "problemMatcher": [],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    },
    // ————————————————
    //  Lancia il task di build per il backend
    // ————————————————
    {
      "label": "🎡 Run BE",
      "type": "shell",
      "command": "node server.js",
      "options": {
        "cwd": "${workspaceFolder}/BE"
      },
      "problemMatcher": [],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
